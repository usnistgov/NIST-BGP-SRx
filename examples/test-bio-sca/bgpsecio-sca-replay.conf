#BGPsec-IO Configuration file. Auto generated by bgpsecio V0.2.1.4
ski_file    = "../opt/bgp-srx-examples/bgpsec-keys/ski-list.txt";
ski_key_loc = "../opt/bgp-srx-examples/bgpsec-keys/";

preload_eckey = false;
# we will use the mode as runtime parameter
# mode = "GEN-B";
# mode = "GEN-C";
# We will add the outfile as runtime parameter 
# out = "/tmp/bgpsecio-sca.dat";


max = 0;
only_extended_length = true;
appendOut = "false";
capi_cfg = "../opt/bgp-srx-examples/test-bio-sca/srxcryptoapi.conf";

session = (
  {
    asn        = 65005;
    bgp_ident  = "10.0.0.5";
    hold_timer = 180;

    peer_asn   = 65000;
    peer_ip    = "10.0.0.10";
    peer_port  = 179;

    # Updates for this session only
    # (path prefix B4 specifies BGP4 only update!)
    # <prefix>[,[[B4]? <asn>[p<repetition>]]*[ ]*[I|V|N]?]
    
    update = ( );
    # Update 1: PASS
    # Update 2: FAIL (No key for 65050)
    # Update 3: PASS
    # Update 4: PASS

    incl_global_updates = false;

    algo_id = 1;
    signature_generation = "BIO";

    # USE A FAKE SIGNATURE WHERE NO KEYS ARE AVAILABLE
    null_signature_mode = "FAKE";
    fake_signature      = "1BADBEEFDEADFEED" "2BADBEEFDEADFEED"
                          "3BADBEEFDEADFEED" "4BADBEEFDEADFEED"
                          "5BADBEEFDEADFEED" "6BADBEEFDEADFEED"
                          "7BADBEEFDEADFEED" "8BADBEEFDEADFEED"
                          "ABADBEEFFACE";
    fake_ski            = "0102030405060708" "090A0B0C0D0E0F10"
                          "11121314";
    printOnInvalid = false;
  }
);
update = ( 
         );
